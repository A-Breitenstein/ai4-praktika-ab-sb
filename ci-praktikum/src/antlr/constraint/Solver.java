package antlr.constraint;

import choco.Choco;
import choco.Options;
import choco.kernel.model.variables.integer.IntegerVariable;

/**
 * User: Alex
 * Date: 25.05.13
 * Time: 06:38
 */
public class Solver {

    IntegerVariable A = Choco.makeIntVar("A", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable B = Choco.makeIntVar("B", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable C = Choco.makeIntVar("C", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable D = Choco.makeIntVar("D", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable E = Choco.makeIntVar("E", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable F = Choco.makeIntVar("F", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable G = Choco.makeIntVar("G", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable H = Choco.makeIntVar("H", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable I = Choco.makeIntVar("I", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable J = Choco.makeIntVar("J", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable K = Choco.makeIntVar("K", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable L = Choco.makeIntVar("L", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable M = Choco.makeIntVar("M", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable N = Choco.makeIntVar("N", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable O = Choco.makeIntVar("O", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable P = Choco.makeIntVar("P", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable Q = Choco.makeIntVar("Q", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable R = Choco.makeIntVar("R", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable S = Choco.makeIntVar("S", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable T = Choco.makeIntVar("T", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable U = Choco.makeIntVar("U", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable V = Choco.makeIntVar("V", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable W = Choco.makeIntVar("W", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable X = Choco.makeIntVar("X", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable Y = Choco.makeIntVar("Y", 0, 9, String.valueOf(Options.V_ENUM));
    IntegerVariable Z = Choco.makeIntVar("Z", 0, 9, String.valueOf(Options.V_ENUM));

    public static void main(String[] args) {
//        for (char buchstabe = 'A'; buchstabe <= 'Z'; buchstabe++) {
////            System.out.println(buchstabe + ", " + (int)buchstabe);
//            System.out.println("IntegerVariable "+ buchstabe+" = Choco.makeIntVar(\""+ buchstabe +"\", 0, 9, String.valueOf(Options.V_ENUM));");
//        }
    }
}
